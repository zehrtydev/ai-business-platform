import { ModulePlaceholder } from '../../../components/module-placeholder';

export default function ServicesPage() {
  return (
    <ModulePlaceholder
      description="Configure the services customers can discover and schedule."
      eyebrow="Business configuration"
      title="Services"
    />
  );
}
