import { ModulePlaceholder } from '../../../components/module-placeholder';

export default function AppointmentsPage() {
  return (
    <ModulePlaceholder
      description="View scheduled activity and, later, manage appointment lifecycle and availability."
      eyebrow="Scheduling"
      title="Appointments"
    />
  );
}
